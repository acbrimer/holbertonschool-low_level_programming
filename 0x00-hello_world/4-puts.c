#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 * prints "Programming is like building a multilingual puzzle
 * Return: Always 0 (Success)
*/

int main(void)
{
	char quote[52];
	strcpy(quote, "\"Programming is like building a multilingual puzzle");
	
	puts(quote);	
	return (0);
}
